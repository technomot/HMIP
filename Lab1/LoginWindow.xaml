<Window x:Class="PasswordProtectionApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Log In - Password Protection System"
        Height="260" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Please enter your user name and password"
                   FontWeight="Bold" FontSize="14" Margin="0,0,0,12"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="User name:" VerticalAlignment="Center"/>
            <TextBox x:Name="UserNameBox" Grid.Column="1" Height="26" VerticalContentAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Password:" VerticalAlignment="Center"/>
            <PasswordBox x:Name="PasswordBoxCtrl" Grid.Column="1" Height="26" VerticalContentAlignment="Center"
                         PasswordChar="*"/>
        </Grid>

        <TextBlock x:Name="StatusText" Grid.Row="3" Foreground="Red" TextWrapping="Wrap" Margin="0,4,0,0"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="LoginButton" Content="Log In" Width="90" Height="28" Margin="0,0,8,0" Click="LoginButton_Click" IsDefault="True"/>
            <Button x:Name="ExitButton" Content="Exit" Width="90" Height="28" Click="ExitButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>